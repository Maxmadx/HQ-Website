import React from 'react';
import WhyFlyAHelicopter from '../components/WhyFlyAHelicopter';

/**
 * Page wrapper at /final-why-fly-a-helicopter.
 * The section itself lives in src/components/WhyFlyAHelicopter.jsx
 * so it can be reused on other pages (e.g. /training/ppl).
 */
const FinalWhyFlyAHelicopter = () => <WhyFlyAHelicopter />;

export default FinalWhyFlyAHelicopter;
