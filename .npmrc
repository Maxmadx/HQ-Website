# react-simple-maps@3.0.0 declares peer react@<20 but project uses react@19.
# Local dev + Dockerfile already use --legacy-peer-deps; this makes the CI
# default match so npm ci doesn't fail. Revisit once react-simple-maps ships
# a React 19-compatible release, or replace it.
legacy-peer-deps=true
